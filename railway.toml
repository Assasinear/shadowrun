[build]
builder = "nixpacks"
buildCommand = "cd apps/admin && pnpm install && pnpm build && cd ../../apps/api && pnpm install && pnpm prisma generate && pnpm build"

[deploy]
startCommand = "cd apps/api && pnpm prisma migrate deploy && node dist/src/main"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
